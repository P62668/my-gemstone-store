// Test runner stub used only for local scripts — not required for production build
export async function runTests() {
  // Intentionally left blank for CI/build. Run tests with `npm test` or `vitest` directly.
  return;
}

// Keep default export for compatibility
export default runTests;
